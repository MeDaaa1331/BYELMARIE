<section class="section" style="background: var(--color-bg-alt); border-top: 1px solid var(--color-border);">
  <div class="container">
    {%- assign label_dc = section.settings.label | downcase -%}
    {%- assign title_dc = section.settings.title | downcase -%}
    {%- assign i18n_label = "" -%}
    {%- assign i18n_title = "" -%}
    {%- if label_dc contains 'e-shop' or label_dc contains 'eshop' or label_dc contains 'shop' -%}
      {%- assign i18n_label = "nav_shop" -%}
    {%- elsif label_dc contains 'školení' or label_dc contains 'skoleni' or label_dc contains 'training' -%}
      {%- assign i18n_label = "pro_label" -%}
    {%- endif -%}
    {%- if title_dc contains 'nejprodávanější' or title_dc contains 'nejprodavanejsi' or title_dc contains 'best' -%}
      {%- assign i18n_title = "products_heading" -%}
    {%- elsif title_dc contains 'profesionální' or title_dc contains 'profesionalni' or title_dc contains 'professional' -%}
      {%- assign i18n_title = "pro_title" -%}
    {%- endif -%}
    <div class="section-header animate-on-scroll">
      <span class="section-header__label" {% if i18n_label != "" %}data-i18n="{{ i18n_label }}"{% endif %}>{{ section.settings.label }}</span>
      <h2 class="section-header__title" {% if i18n_title != "" %}data-i18n="{{ i18n_title }}"{% endif %}>{{ section.settings.title }}</h2>
    </div>

    {%- assign collection = collections[section.settings.collection] -%}
    {%- assign limit = section.settings.limit -%}

    <div class="product-grid">
      {%- if collection != blank and collection.products_count > 0 -%}
        {%- for product in collection.products limit: limit -%}
          {%- render 'product-card', product: product -%}
        {%- endfor -%}
      {%- else -%}
        {% comment %} Fallback v editaci, pokud není vybrána kolekce {% endcomment %}
        {%- for i in (1..limit) -%}
          <article class="product-card">
            <div class="product-card__image-wrap">
              <img src="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' width='400' height='400'><rect width='400' height='400' fill='%23f0ede8'/><text x='50%25' y='50%25' text-anchor='middle' fill='%23cdc1b3' font-size='18' dy='.3em'>Demo Produkt {{ i }}</text></svg>" class="product-card__image">
            </div>
            <div class="product-card__info">
              <a href="#" class="product-card__name">Demo Produkt {{ i }}</a>
              <div class="product-card__price-row">
                <span class="price">990 Kč</span>
                <span class="btn-add-to-cart">Do košíku</span>
              </div>
            </div>
          </article>
        {%- endfor -%}
      {%- endif -%}
    </div>

    {%- if section.settings.show_view_all and collection != blank -%}
      <div class="section-footer animate-on-scroll">
        <a href="{{ collection.url }}" class="btn btn-outline" data-i18n="products_all">{{ section.settings.view_all_label }}</a>
      </div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Doporučené produkty",
  "settings": [
    {
      "type": "text",
      "id": "label",
      "label": "Označení sekce",
      "default": "E-shop"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Nadpis sekce",
      "default": "Nejprodávanější produkty"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Vybrat kolekci"
    },
    {
      "type": "range",
      "id": "limit",
      "min": 2,
      "max": 8,
      "step": 1,
      "default": 4,
      "label": "Limit produktů k zobrazení"
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "label": "Zobrazit tlačítko 'Zobrazit vše'",
      "default": true
    },
    {
      "type": "text",
      "id": "view_all_label",
      "label": "Text tlačítka",
      "default": "Zobrazit vše"
    }
  ],
  "presets": [
    {
      "name": "Doporučené produkty"
    }
  ]
}
{% endschema %}
